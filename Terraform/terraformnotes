terraform workspace
terraform workspace list
terraform workspace new (workspace name)
terraform workspace select (workspace name)
terraform workspace delete (workspace  name)
terraform plan/apply --var-file (\dev.tfvars)===> File that contains variable declarations which have to be used for particular environment
terraform destroy --var-file (\filename)


$env:TF_LOG="TRACE" --- $env:TF_LOG=""

$env:TF_LOG_PATH="terraform.log" ----$env:TF_LOG_PATH=""